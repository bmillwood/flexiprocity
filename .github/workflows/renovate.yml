on:
  schedule:
  - cron: "30 17 * * 5" # Friday afternoon
  workflow_dispatch:
  pull_request:
    paths:
    - .github/renovate.json
    - .github/workflows/renovate.yml

jobs:
  upgrade-dependencies:
    uses: bmillwood/renovateconfig/.github/workflows/renovate.yml@main
    secrets: inherit
