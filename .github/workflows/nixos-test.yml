on:
  push:
    branches:
      - main
  pull_request:
    paths:
      - .github/workflows/nixos-test.yml
      - tests/**
      - config.nix
      - schema/config.nix
      - schema/structure.sql
      - postgraphile/**
      - secrets/new-jwt-key.sh
      - agent/**
      - auth-server/**
      - frontend/**

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash -euo pipefail {0}

    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - uses: cachix/install-nix-action@ab739621df7a23f52766f9ccc97f38da6b7af14f # v31
        with:
          nix_path: nixpkgs=channel:nixos-unstable

      - run: nix-build tests/module.nix --no-out-link
