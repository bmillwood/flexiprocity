{
    "extends": [
        "config:recommended"
    ],
    "pinDigests": true,
    "customManagers": [
        {
            "customType": "regex",
            "description": "Update *-version parameters",
            "managerFilePatterns": [".github/workflows/*"],
            "matchStrings": [
                "-version:\\s*(?<currentValue>[0-9.]+)\\s*# renovate: datasource=(?<datasource>[a-z-]+) depName=(?<depName>\\S+)"
            ]
        }
    ]
}
